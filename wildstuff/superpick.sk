#superpick
on right click with pickaxe:
	name of player's tool is "&fSuper Pick":
		set {_type} to type of targeted block
		loop blocks in radius 10 of targeted block:
			loop-block is {_type}
			delete loop-block
			
			
command /pickodoom:
	trigger:
		give player pickaxe named "&fSuper Pick"